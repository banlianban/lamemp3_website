# Docker 接入完成总结

本文档总结了 HDR Ultra Web 项目的 Docker 配置，方便快速了解和使用。

## ✅ 已完成的工作

### 1. 核心 Docker 文件

- ✅ `Dockerfile` - 生产环境多阶段构建配置
- ✅ `Dockerfile.dev` - 开发环境配置
- ✅ `.dockerignore` - Docker 构建忽略文件
- ✅ `docker-compose.yml` - 生产环境编排（简单模式）
- ✅ `docker-compose.dev.yml` - 开发环境编排
- ✅ `docker-compose.prod.yml` - 生产环境编排（带 Nginx）

### 2. Nginx 配置

```
nginx/
├── nginx.conf              # Nginx 主配置
├── conf.d/
│   └── default.conf       # 站点配置
└── logs/                  # 日志目录
    └── .gitkeep
```

- ✅ HTTP 服务器配置
- ✅ 反向代理到 Next.js 应用
- ✅ 静态文件缓存优化
- ✅ Gzip 压缩配置
- ✅ 安全头部设置
- ✅ HTTPS 配置模板（可选）

### 3. 辅助脚本

```
scripts/
├── docker-check.sh        # 环境检查脚本
└── docker-deploy.sh       # 快速部署脚本
```

- ✅ 自动检查 Docker 环境
- ✅ 一键部署命令
- ✅ 彩色输出和友好提示

### 4. Makefile 命令

```bash
make help        # 查看所有命令
make dev         # 开发环境
make up          # 生产环境（简单）
make prod-up     # 生产环境（带 Nginx）
make logs        # 查看日志
make down        # 停止服务
make clean       # 清理资源
```

### 5. 文档

```
docs/development/
├── DOCKER.md                    # 完整部署指南
├── DOCKER-CHECKLIST.md          # 部署检查清单
└── DOCKER-QUICK-REFERENCE.md    # 快速命令参考
```

### 6. CI/CD

- ✅ `.github/workflows/docker-build.yml` - GitHub Actions 自动构建

### 7. 项目配置更新

- ✅ `next.config.mjs` - 启用 standalone 输出模式
- ✅ `README.md` - 添加 Docker 部署说明
- ✅ `.gitignore` - 忽略 Docker 相关文件
- ✅ `.env.example` - 环境变量模板

## 🚀 快速开始

### 方法一：使用脚本（推荐新手）

```bash
# 1. 检查环境
./scripts/docker-check.sh

# 2. 选择部署模式
./scripts/docker-deploy.sh dev        # 开发环境
./scripts/docker-deploy.sh prod       # 生产环境
./scripts/docker-deploy.sh prod-nginx # 生产 + Nginx
```

### 方法二：使用 Makefile（推荐）

```bash
# 查看帮助
make help

# 开发环境
make dev

# 生产环境
make up

# 生产环境 + Nginx
make prod-up
```

### 方法三：直接使用 Docker Compose

```bash
# 开发环境
docker-compose -f docker-compose.dev.yml up -d

# 生产环境（简单）
docker-compose up -d

# 生产环境（带 Nginx）
docker-compose -f docker-compose.prod.yml up -d
```

## 📋 部署模式对比

| 特性 | 开发环境 | 生产环境（简单） | 生产环境（Nginx） |
|-----|---------|----------------|------------------|
| **文件** | docker-compose.dev.yml | docker-compose.yml | docker-compose.prod.yml |
| **端口** | 3000 | 3000 | 80/443 |
| **热重载** | ✅ 支持 | ❌ 不支持 | ❌ 不支持 |
| **镜像优化** | ❌ 未优化 | ✅ 优化 | ✅ 优化 |
| **反向代理** | ❌ 无 | ❌ 无 | ✅ Nginx |
| **静态缓存** | ❌ 无 | ❌ 无 | ✅ 有 |
| **Gzip 压缩** | ❌ 无 | ❌ 无 | ✅ 有 |
| **HTTPS** | ❌ 不支持 | ❌ 不支持 | ✅ 可配置 |
| **适用场景** | 本地开发 | 简单部署 | 生产部署 |

## 🏗️ 架构说明

### 开发环境架构

```
┌─────────────────────────────────┐
│   Developer                     │
└──────────┬──────────────────────┘
           │ :3000
           ▼
┌─────────────────────────────────┐
│   Docker Container              │
│   ┌─────────────────────────┐   │
│   │   Next.js Dev Server    │   │
│   │   (Hot Reload)          │   │
│   └─────────────────────────┘   │
│   Volume Mounted: /app/src      │
└─────────────────────────────────┘
```

### 生产环境架构（简单）

```
┌─────────────────────────────────┐
│   User                          │
└──────────┬──────────────────────┘
           │ :3000
           ▼
┌─────────────────────────────────┐
│   Docker Container              │
│   ┌─────────────────────────┐   │
│   │   Next.js Server        │   │
│   │   (Standalone)          │   │
│   └─────────────────────────┘   │
└─────────────────────────────────┘
```

### 生产环境架构（带 Nginx）

```
┌─────────────────────────────────┐
│   User                          │
└──────────┬──────────────────────┘
           │ :80/:443
           ▼
┌─────────────────────────────────┐
│   Nginx Container               │
│   - 反向代理                     │
│   - 静态文件缓存                 │
│   - Gzip 压缩                   │
│   - SSL/TLS                     │
└──────────┬──────────────────────┘
           │ :3000 (internal)
           ▼
┌─────────────────────────────────┐
│   Next.js Container             │
│   ┌─────────────────────────┐   │
│   │   Next.js Server        │   │
│   │   (Standalone)          │   │
│   └─────────────────────────┘   │
└─────────────────────────────────┘
```

## 🎯 镜像优化

### 多阶段构建

```dockerfile
Stage 1: deps      → 安装依赖
Stage 2: builder   → 构建应用
Stage 3: runner    → 运行应用（最终镜像）
```

### 优化效果

- ✅ 使用 Alpine Linux（轻量级）
- ✅ 仅包含生产依赖
- ✅ 利用 Next.js standalone 模式
- ✅ 非 root 用户运行
- ✅ 多层缓存优化

**预期镜像大小:** ~300-500MB（含 Node.js 运行时和应用代码）

## 🔧 配置说明

### 环境变量

创建环境变量文件：

```bash
cp .env.example .env.local          # 开发环境
cp .env.example .env.production     # 生产环境
```

在 `docker-compose.yml` 中启用：

```yaml
env_file:
  - .env.production
```

### 端口修改

如果端口冲突，修改对应的 `docker-compose.yml`：

```yaml
ports:
  - "3001:3000"  # 主机端口:容器端口
```

### 资源限制

已在 `docker-compose.prod.yml` 中配置：

```yaml
deploy:
  resources:
    limits:
      cpus: '1.0'
      memory: 1G
```

### HTTPS 配置

1. 将 SSL 证书放到 `nginx/ssl/` 目录
2. 编辑 `nginx/conf.d/default.conf`
3. 取消 HTTPS 服务器配置的注释

## 📊 性能特性

### Nginx 优化

- ✅ 静态文件缓存（1年）
- ✅ Gzip 压缩（文本、JS、CSS）
- ✅ HTTP/2 支持
- ✅ Keep-Alive 连接
- ✅ 缓冲区优化

### 健康检查

生产环境配置了容器健康检查：

```yaml
healthcheck:
  test: ["CMD", "node", "-e", "..."]
  interval: 30s
  timeout: 10s
  retries: 3
```

## 🛡️ 安全特性

- ✅ 非 root 用户运行（uid: 1001）
- ✅ 安全 HTTP 头部
  - X-Frame-Options
  - X-Content-Type-Options
  - X-XSS-Protection
  - Referrer-Policy
  - HSTS（HTTPS 时）
- ✅ 最小化镜像攻击面
- ✅ 无敏感信息泄露

## 📚 文档索引

### 新手指南

1. [DOCKER.md](./docs/development/DOCKER.md) - 从零开始的完整指南
2. [DOCKER-CHECKLIST.md](./docs/development/DOCKER-CHECKLIST.md) - 部署前后检查清单
3. [README.md](./README.md#docker-部署) - 项目主文档

### 进阶参考

1. [DOCKER-QUICK-REFERENCE.md](./docs/development/DOCKER-QUICK-REFERENCE.md) - 命令速查表
2. [Dockerfile](./Dockerfile) - 生产环境构建配置
3. [nginx/conf.d/default.conf](./nginx/conf.d/default.conf) - Nginx 配置

## 🔄 CI/CD 集成

已配置 GitHub Actions 自动构建：

- ✅ 推送到 main 分支时自动构建
- ✅ 打 tag 时自动发布版本
- ✅ Pull Request 时构建测试
- ✅ 支持多架构（amd64/arm64）
- ✅ 使用 GitHub Container Registry

**触发条件:**
- `git push origin main`
- `git tag v1.0.0 && git push --tags`

## 🎓 最佳实践

### 开发流程

```bash
# 1. 启动开发环境
make dev

# 2. 代码修改（自动热重载）

# 3. 本地测试
open http://localhost:3000

# 4. 停止
make dev-down
```

### 生产部署流程

```bash
# 1. 检查环境
./scripts/docker-check.sh

# 2. 拉取最新代码
git pull origin main

# 3. 构建镜像
make prod-build

# 4. 启动服务
make prod-up

# 5. 验证部署
curl http://localhost

# 6. 查看日志
make prod-logs
```

### 更新流程

```bash
# 1. 拉取更新
git pull

# 2. 重新构建
docker-compose -f docker-compose.prod.yml build --no-cache

# 3. 重启服务（零停机）
docker-compose -f docker-compose.prod.yml up -d
```

## 🐛 常见问题

### Q: 如何查看日志？

```bash
# 实时日志
make logs
make prod-logs

# 或
docker logs -f hdr-ultra-web
```

### Q: 容器启动失败怎么办？

```bash
# 查看错误信息
docker logs hdr-ultra-web

# 检查容器状态
docker ps -a

# 重新构建
make build
```

### Q: 如何进入容器调试？

```bash
docker exec -it hdr-ultra-web sh
```

### Q: WASM 文件无法加载？

检查 `.dockerignore` 确保 `public/wasm/` 没有被排除。

### Q: 如何配置 HTTPS？

参考 `nginx/conf.d/default.conf` 中的 HTTPS 配置模板。

## 🚀 下一步

### 推荐配置（生产环境）

1. ✅ 配置域名和 HTTPS
2. ✅ 设置防火墙规则
3. ✅ 配置日志轮转
4. ✅ 集成监控系统（Prometheus + Grafana）
5. ✅ 配置自动备份
6. ✅ 设置 CI/CD 自动部署

### 可选增强

- [ ] 添加 Redis 缓存
- [ ] 集成 CDN
- [ ] 添加速率限制
- [ ] 配置 WAF
- [ ] 多副本部署（负载均衡）

## 📞 获取帮助

如果遇到问题：

1. 查看 [Docker 文档](./docs/development/DOCKER.md)
2. 查看 [故障排查清单](./docs/development/DOCKER-CHECKLIST.md#故障排查)
3. 查看容器日志 `docker logs hdr-ultra-web`
4. 提交 Issue

## 📝 备注

- 所有脚本都已添加执行权限
- 默认使用 3000 端口（开发/生产简单模式）
- Nginx 模式使用 80/443 端口
- 数据不持久化（无状态应用）
- WASM 文件需要正确放置在 `public/wasm/` 目录

---

**Docker 接入完成！** 🎉

现在你可以使用 Docker 轻松部署和运行 HDR Ultra Web 项目了。

建议先从开发环境开始熟悉，然后再部署到生产环境。

